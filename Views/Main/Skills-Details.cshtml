@using Entity.Context

@{
    DatabaseContext db = new DatabaseContext();
    var skills = db.Skills.ToList();
}

@foreach (var item in skills)
{
    <div class="portfolio-modal modal fade" id="portfolioModal-@item.Id" tabindex="-1" aria-labelledby="portfolioModal-@item.Id" aria-hidden="true">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header border-0"><button class="btn-close" type="button" data-bs-dismiss="modal" aria-label="Close"></button></div>
                <div class="modal-body text-center pb-5">
                    <div class="container">
                        <div class="row justify-content-center">
                            <div class="col-lg-8">
                                <h3 class="portfolio-modal-title text-secondary text-uppercase mb-0">@item.SkillTitle</h3><br /><br />
                                <p class="mb-4">@item.Description</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}